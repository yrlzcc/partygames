<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="partygames.shirley.com.undercoverlib">

    <uses-permission android:name="android.permission.VIBRATE"/> <!-- 使用振动 -->
    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
>

        <activity android:name=".UndercoverActivity"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".UnderCoverSetting"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".UnderCoverGuess"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".UnderBaseActivity"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".UnderPunishActivity"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>
